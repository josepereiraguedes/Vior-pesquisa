<div align="center">
  <h1>🌸 Vior Store - Pesquisa de Preferências & Insights</h1>
  <p>
    <b>Sistema robusto de inteligência de mercado para o lançamento da marca Vior Store.</b><br/>
    Coleta dados, detecta tendências com IA e engaja leads com sorteios e cupons.
  </p>
</div>

## ✨ Funcionalidades

- **Frontend**: React + Vite + Tailwind CSS (Rápido e Responsivo).
- **Backend**: Supabase (Armazenamento em tempo real + Autenticação).
- **Inteligência Artificial**: Google Gemini (Análise semântica de preferências).
- **Gamificação**:
  - Geração de cupons únicos (`MIAMO`, `DIVA`).
  - Sorteio digital integrado.
  - Exportação de dados para Excel/CSV.

## 🚀 Como Rodar Localmente

1. **Clone o repositório**
   ```bash
   git clone https://github.com/seu-usuario/vior-pesquisa.git
   cd vior-pesquisa
   ```

2. **Instale as dependências**
   ```bash
   npm install
   ```

3. **Configure as Variáveis de Ambiente**
   - Copie o arquivo de exemplo: `cp .env.example .env`
   - Preencha o `.env` com suas chaves do Supabase e Gemini.

4. **Rode o projeto**
   ```bash
   npm run dev
   ```

## 🔐 Acesso Administrativo

Acesse a rota do Dashboard clicando no cadeado no rodapé ou indo para `/?page=dashboard`.
> **PIN Padrão**: Configure no `.env` (padrão: 1234).

---

Desenvolvido para **Vior Store** com ❤️.
